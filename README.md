```rust
fn readme(n: i32) -> &'static str {
  match n {
    1 => { "Go, Python, Rust, C++" }
    2 => { "ctfs w/ Cosmic Bit Flip" }
    3 => { "incoming cs @ georgia tech" }
    _ => { ":)" }
  }
}
```
